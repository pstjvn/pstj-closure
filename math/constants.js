/**
 * @fileoverview Some constants we will be using in calculations.
 * This is not strictly needed but makes no sense to constantly
 * thinking about it in code. Instead we place them here and use them
 * as constants.
 *
 * @author regardingscot@gmail.com (Peter StJ)
 */
goog.provide('pstj.math.constants');


/** @const {number} */
pstj.math.constants.ZeroDegree = Math.PI + (Math.PI / 2);


/** @const {number} */
pstj.math.constants.CircleCircumference = Math.PI * 2;


/** @const {number} */
pstj.math.constants.OneDegree = Math.PI / 180;


/** @const {number} */
pstj.math.constants.TwoDegrees = (Math.PI / 180) * 2;