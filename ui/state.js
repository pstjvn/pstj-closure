goog.provide('pstj.ui.State');

/**
 * Some additional states we will need to implement better touchables.
 * @enum {number}
 */
pstj.ui.State = {
  PRESSED: 0x80,
  MOVED: 0x100,
  LONG_PRESSED: 0x200
};
